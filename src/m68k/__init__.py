"""Motorola 68k Binary Ninja plugin implementation.

The Binary Ninja plugin entrypoint lives at the repository root `__init__.py`.
This package exists to support a `src/` layout and normal imports during unit
testing (via `uv run pytest`).
"""

